import Layout from "../layouts/Layout";

const About: React.FC = () => {
  return (
    <Layout>
      <h1>About Page</h1>
      <p>This is a boilerplate using Vite, React, TypeScript, and Emotion.</p>
    </Layout>
  );
};

export default About;